import { RoutePlaceholder } from '@/components/shell/RoutePlaceholder';

/** Attendance. Placeholder body; the shell around it is the deliverable here. */
export default function AttendancePage() {
  return <RoutePlaceholder titleKey='nav.attendance' />;
}
