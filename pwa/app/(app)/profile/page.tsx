import { RoutePlaceholder } from '@/components/shell/RoutePlaceholder';

/** Profile. Placeholder body; the shell around it is the deliverable here. */
export default function ProfilePage() {
  return <RoutePlaceholder titleKey='nav.profile' />;
}
