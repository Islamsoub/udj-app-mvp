import { RoutePlaceholder } from '@/components/shell/RoutePlaceholder';

/** Dashboard. Placeholder body; the shell around it is the deliverable here. */
export default function DashboardPage() {
  return <RoutePlaceholder titleKey='nav.dashboard' />;
}
