# Base URL of the Unipocket Express API, no trailing slash.
NEXT_PUBLIC_API_URL=""

# Server-only shared secret matching the backend's PWA_PROXY_SECRET; leave empty to disable forwarded-IP rate limiting.
PWA_PROXY_SECRET=""
